[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proxy-pool-demo"
version = "0.1.0"
description = "从 0 到 1 搭建自用代理池 - Demo"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "redis>=5.0",
    "flask>=3.0",
]

[tool.setuptools.packages.find]
include = []